server:
  port: 8081

spring:
  profiles:
    active: local
  datasource:
    driver-class-name: org.postgresql.Driver
    username: test
    password: test
    url: jdbc:postgresql://localhost:5432/test
  jpa:
    hibernate:
      ddl-auto: update

feign:
  url:
    goods: localhost:8080